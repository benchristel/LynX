#!/bin/bash -e

lib/manual.sh "In Firefox, navigate to about:config, search for ui.key.menuAccessKeyFocuses, and set the Value to false."
lib/manual.sh "In Firefox's settings, change the default font to Times New Roman"
